import React from 'react';
import './Hero.css'; // Create and style this CSS file accordingly
 // Add your hero image to the assets folder
import SwiperSlider from '../Slider/SwiperSlider';

const Hero = () => {
  return (
    <section id="hero" className="hero">
      <SwiperSlider/>

      
    </section>
  );
};

export default Hero;